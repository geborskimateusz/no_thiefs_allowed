version: '3.8'

services:
  server:
    build: ./server
    ports:
      - "5000:5000"
    volumes:
      - ./server:/app
  app:
    build: ./app
    ports:
      - "3000:3000"
    depends_on:
      - server 

