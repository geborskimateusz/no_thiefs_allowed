version: '3.8'

services:
  web:
    build: .
    environment:
      - API_AUTH_TOKEN=${API_AUTH_TOKEN}
    expose:
      - "5000"

  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - web

